const scores = {
  energy: 50,
  full: 100,
  strength: 100,
  fun: 50,
};

const getScores = () => {
  const allTheScores = scores;
  return allTheScores;
};

export default { getScores };
console.error('energy SCORES:', scores.energy);
